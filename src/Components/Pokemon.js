//THIS COMPONENT IS CURRENTLY UNUSED IN THE APPLICATION
const Pokemon = (props) => {
    const {name} = props.pokemon
    return(
        <div>
            <h1>{name}</h1>
        </div>
    )
}

export default Pokemon